<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaipeiZoo" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.TaipeiZoo" parent="Base.Theme.TaipeiZoo" />

    <style name="Widget.App.Toolbar.Surface" parent="Widget.Material3.Toolbar.Surface">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Toolbar.Surface</item>
    </style>

    <style name="ThemeOverlay.App.Toolbar.Surface" parent="ThemeOverlay.Material3.Toolbar.Surface">
        <item name="elevationOverlayEnabled">false</item>
    </style>

    <style name="Theme.TaipeiZoo.SplashScreen" parent="Theme.TaipeiZoo">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>

        <!-- Set the splash screen background, animated icon, and animation
   duration. -->
        <item name="windowSplashScreenBackground">@color/white</item>

        <!-- Use windowSplashScreenAnimatedIcon to add a drawable or an animated
             drawable. One of these is required. -->
        <!--        <item name="windowSplashScreenAnimatedIcon">@drawable/...</item>-->
        <!-- Required for animated icons. -->
        <item name="windowSplashScreenAnimationDuration">200</item>

        <!-- Set the theme of the Activity that directly follows your splash
        screen. This is required. -->
        <!--        <item name="postSplashScreenTheme">@style/Theme.App</item>-->
    </style>

    <style name="ShapeAppearanceOverlayRound" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">20dp</item>
        <item name="cornerSizeTopRight">20dp</item>
    </style>

    <style name="ModalBottomSheet" parent="Widget.Material3.BottomSheet.Modal">
        <item name="shapeAppearance">@style/ShapeAppearanceOverlayRound</item>
        <item name="android:layout_marginTop">150dp</item>
        <item name="backgroundTint">@color/primary</item>
    </style>

    <style name="Material3BottomSheetDialogRoundCorner" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/ModalBottomSheet</item>
    </style>

    <style name="MyBottomNavigationActiveIndicatorStyle" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:textSize">12sp</item>  <!-- 調整文字大小 -->
    </style>

    <style name="MyBottomNavigationInactiveItemTextAppearance" parent="TextAppearance.Material3.BodySmall">
        <item name="android:textSize">12sp</item>  <!-- 調整文字大小 -->
    </style>
</resources>